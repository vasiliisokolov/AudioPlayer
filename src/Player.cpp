#include "Player.h"

class Player
{
	std::map<std::string, Track> playList{};
	bool play = false;

public:
	void setList(std::map<std::string, Track>& playList)
	{
		Track* track = new Track();
	}


};